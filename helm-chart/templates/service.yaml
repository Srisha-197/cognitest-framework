apiVersion: v1
kind: Service
metadata:
  name: cognitest-engine
spec:
  selector:
    app: cognitest-engine
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 3000
  type: {{ .Values.service.type }}
